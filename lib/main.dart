import 'package:flutter/material.dart';

import 'hooks_example/main_hooks.dart';
import 'mobx_example/mobx_main.dart';
import 'provider_example/main_provider.dart';

void main() {
  // Uncomment to use the BloC Example
  // runApp(BlocApp());
  // Uncomment to use the Provider Example
  //runApp(ProviderApp());
  // Uncomment to use the Hooks Example
  //runApp(HooksApp());
  // Uncomment to use the MobX Example
  runApp(MobxApp());
}
